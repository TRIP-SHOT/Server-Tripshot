<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tripshot.map.mapper.TripPlanMapper">

	<select id="searchByKeyword" resultType="AttractionInfo">
		select *
		from attraction_info
		where title like concat('%', #{keyword}, '%')
	</select>
	
	<insert id="addPlan">
		insert into plan(user_id)
		values (#{user_id})
	</insert>
	
	<select id="getPlan">
		select max(plan_id)
		from plan
		where user_id = #{user_id}
	</select>
	
	<insert id="addPlanDetail" parameterType="PlanDetail">
		insert into plan_detail(plan_id, attraction_id, memo)
		values(#{plan_id}, #{attraction_id}, #{memo})
	</insert>
	
</mapper>